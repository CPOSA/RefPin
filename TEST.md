# PinRef 测试清单

这份文档用于阶段验收和跨平台回归测试。每次发布或完成一个阶段后，至少在
macOS 和 Windows 各执行一轮，并把测试环境、结果和缺陷记录在文档末尾。

结果标记：

- `[ ]` 未测试
- `[x]` 通过
- `[!]` 失败或存在异常
- `[-]` 当前平台不适用

---

## 1. 测试环境记录

每轮测试先填写一份环境信息：

```text
测试日期：
测试人：
Git commit：
操作系统及版本：
Python 版本：
PySide6 版本：
CPU / 内存：
显示器数量：
各显示器分辨率与缩放比例：
显示器排列方式（左 / 右 / 上 / 下）：
截图后端（qt / mss）：
```

Windows 至少覆盖 Windows 10、Windows 11 中当前计划支持的版本；macOS 至少记录
系统版本、Intel 或 Apple Silicon，以及是否授予屏幕录制权限。

---

## 2. 安装与启动

### macOS

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python -m pinref.main
```

- [ ] 依赖安装完成，没有平台不兼容错误
- [ ] `pyobjc-framework-Cocoa` 正常安装
- [ ] 程序可以启动并进入框选状态
- [ ] 首次运行时能正确处理屏幕录制权限
- [ ] 关闭最后一张浮窗后，进程正常退出

### Windows PowerShell

```powershell
py -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python -m pinref.main
```

- [ ] 依赖安装完成，没有尝试安装 macOS 专属的 PyObjC
- [ ] 程序可以启动并进入框选状态
- [ ] 没有缺少 DLL、Qt 平台插件或 NumPy wheel 的错误
- [ ] 关闭最后一张浮窗后，进程正常退出
- [ ] 命令行中没有持续出现影响使用的异常或警告

---

## 3. 自动化测试

在已激活虚拟环境的项目根目录运行：

```bash
python tests/test_imaging.py
python tests/test_floating.py
python tests/test_overlay.py
```

- [ ] 所有图像处理测试通过
- [ ] 所有浮窗与控制面板交互测试通过
- [ ] macOS / Windows 多屏截图坐标换算测试通过
- [ ] RGB 与 QPixmap 往返没有像素损失
- [ ] 已知颜色的色相旋转结果正确
- [ ] 灰度 0%、50%、100% 的结果正确
- [ ] 灰度 100% 时，色相不改变原图明暗关系
- [ ] 色相在 `-180°`、`180°` 及环绕边界处正确
- [ ] 不同设备像素比下，图片逻辑尺寸保持不变
- [ ] sRGB、Display P3 等色彩空间标记得到保留
- [ ] 单像素、非正方形和奇数宽度图片不会崩溃

补充静态检查：

```bash
python -m compileall -q pinref tests
ruff check pinref tests
ruff format --check pinref tests
git diff --check
```

- [ ] Python 编译检查通过
- [ ] Ruff 代码检查通过
- [ ] Ruff 格式检查通过
- [ ] Git 空白字符检查通过

---

## 4. 阶段一：框选与截图

### 基础框选

- [ ] 启动后每块显示器都出现半透明遮罩
- [ ] 鼠标指针显示为十字光标
- [ ] 从左上向右下拖动可以正常框选
- [ ] 从右下向左上拖动可以正常框选
- [ ] 四个拖动方向得到一致的宽高
- [ ] 框选尺寸提示实时更新且数值正确
- [ ] 选中区域透明，区域外正常变暗
- [ ] 小于最小尺寸的误点不会生成浮窗
- [ ] 框选时按 Esc 可以取消并退出
- [ ] 框选时单击右键可以取消并退出

### 截图正确性

- [ ] 截图内容与框选区域完全一致
- [ ] 截图不包含半透明遮罩、选框或尺寸文字
- [ ] 截图没有整体偏移、裁切或额外边缘
- [ ] 红、绿、蓝通道没有交换
- [ ] 高 DPI / Retina 屏幕上的截图清晰度正确
- [ ] 截图所得浮窗逻辑尺寸与原框选区域一致
- [ ] 截取快速变化的视频画面时得到当前画面，而不是旧缓存
- [ ] 截取普通窗口、桌面、浏览器和绘画软件均正常

### 多显示器

- [ ] 主显示器框选和截图正确
- [ ] 副显示器框选和截图正确
- [ ] 位于主屏左侧、具有负坐标的副屏截图正确
- [ ] 位于主屏右侧的副屏截图正确
- [ ] 位于主屏上方或下方的副屏截图正确
- [ ] 不同显示器上的遮罩没有重叠或自动跳屏
- [ ] 一次框选被限制在当前显示器内
- [ ] 当前“不支持跨屏框选”的限制表现稳定，不会崩溃

---

## 5. 阶段一：浮动参考图

- [ ] 截图后浮窗出现在原框选位置
- [ ] 浮窗无标题栏、无系统边框
- [ ] 浮窗保持在普通应用窗口之上
- [ ] 左键拖动浮窗时，鼠标与窗口相对位置不跳变
- [ ] 浮窗可以在同一显示器内自由移动
- [ ] 浮窗可以移动到另一块显示器
- [ ] 在不同缩放比例显示器之间移动时，尺寸和清晰度可接受
- [ ] 右键单击浮窗可以关闭
- [ ] 浮窗获得焦点时按 Esc 可以关闭
- [ ] 关闭浮窗时关联的控制面板同时关闭
- [ ] 关闭最后一张浮窗后程序退出

---

## 6. 阶段二：调色控制面板

### 打开、关闭与定位

- [ ] 双击浮窗可以打开控制面板
- [ ] 再次双击浮窗可以收起控制面板
- [ ] 在面板中按 Esc 只关闭面板，不关闭浮窗
- [ ] 控制面板始终置顶
- [ ] 控制面板可以拖动
- [ ] 拖动浮窗时，控制面板会跟随浮窗
- [ ] 浮窗靠近屏幕右侧时，面板自动放到左侧
- [ ] 浮窗接近全屏时，面板仍完整处于屏幕可用区域
- [ ] 浮窗靠近顶部、底部和任务栏时，面板不会被遮挡或移出屏幕
- [ ] 面板关闭后重新打开，三个滑块保持原值
- [ ] 面板重新打开后，滑块数值与实际图片状态一致

### 灰度

- [ ] 灰度 0% 与原图一致
- [ ] 灰度 100% 为纯黑白，三个 RGB 通道相等
- [ ] 灰度中间值变化连续，没有闪烁或突然跳色
- [ ] 灰度 100% 时移动色相滑块，明暗关系不变化
- [ ] 恢复灰度 0% 后，原始彩色内容正确恢复

### 色相

- [ ] 色相滑块范围为 `-180° ~ 180°`
- [ ] 正向和反向旋转均连续、方向正确
- [ ] `-180°` 与 `180°` 的结果一致
- [ ] 黑、白、灰等中性色不会因色相旋转产生彩色
- [ ] 色相归零后，颜色正确恢复
- [ ] 快速来回拖动时没有崩溃、花屏或遗留旧帧

### 透明度

- [ ] 不透明度范围为 20%～100%
- [ ] 100% 时窗口完全不透明
- [ ] 20% 时仍能看清图片并正常拖动、双击和关闭
- [ ] 调整透明度不会改变图片像素颜色
- [ ] 单独调整透明度不会导致图片变糊
- [ ] 调整透明度后收起并重新打开面板，数值保持一致
- [ ] 半透明大窗口移动和刷新时没有严重卡顿或残影

### 组合调节与状态恢复

- [ ] 同时设置灰度、色相和透明度时结果稳定
- [ ] 快速连续修改多个滑块时，以最后一次参数为准
- [ ] 将灰度和色相恢复为默认值后，图片逐像素恢复原图
- [ ] 收起面板、移动浮窗、重新打开面板后，调节状态不丢失
- [ ] 调节过程中关闭面板不会留下低清预览或半成品画面
- [ ] 调节后关闭浮窗不会发生崩溃或后台线程异常

---

## 7. Windows 专项测试

### 单显示器 DPI

依次在以下缩放比例执行完整的“框选 → 截图 → 调色 → 关闭”流程：

- [ ] 100%
- [ ] 125%
- [ ] 150%
- [ ] 175%
- [ ] 200%（设备支持时）

每个比例都要检查选框位置、截图内容、浮窗逻辑尺寸和调色后尺寸。

### 混合 DPI 多显示器

至少测试这些组合：

- [ ] 主屏 100% + 副屏 100%
- [ ] 主屏 150% + 副屏 100%
- [ ] 主屏 100% + 副屏 150%
- [ ] 主屏 150% + 副屏 200%
- [ ] 主屏和副屏互换后重复测试

每个组合都要在每块屏上分别截图，并把浮窗来回拖动。

### Windows 窗口行为

- [ ] `WindowStaysOnTopHint` 在普通窗口上方生效
- [ ] Photoshop / Clip Studio Paint 等最大化窗口中保持置顶
- [ ] 无边框全屏应用中保持置顶（独占全屏除外）
- [ ] 控制面板透明圆角区域显示正常
- [ ] 改变窗口透明度后没有黑边、黑底或闪烁
- [ ] 任务栏中没有不可接受的重复窗口按钮
- [ ] Alt+Tab 中的窗口数量和行为可接受
- [ ] 任务栏位于屏幕上、下、左、右时，面板均不会被遮挡
- [ ] 从睡眠、锁屏或显示器热插拔恢复后，程序不崩溃

### Windows 已知系统限制

以下场景不作为普通功能失败，但应记录实际表现：

- UAC 安全桌面
- DRM 或受保护视频内容
- 独占全屏游戏
- 禁止屏幕捕获的应用窗口
- 远程桌面或虚拟机中的特殊显示驱动

---

## 8. macOS 专项测试

- [ ] 未授权屏幕录制时，能够识别权限问题而不是误判为普通截图错误
- [ ] 授权并重启终端或编辑器后，能够截取其他应用窗口
- [ ] 程序使用 accessory 策略，不出现在程序坞和 Cmd+Tab 中
- [ ] 遮罩和浮窗能显示在其他应用的全屏 Space 中
- [ ] 主屏和副屏处于不同 Space 时均可框选
- [ ] 浮窗在副屏停留至少 2 秒，不会自动跳到主屏
- [ ] Retina 截图使用原生清晰度
- [ ] 菜单栏和程序坞不会遮挡控制面板
- [ ] Intel Mac 与 Apple Silicon 均无平台依赖错误（设备可用时）

---

## 9. 性能与压力测试

测试时记录初始化耗时、单次调色耗时、CPU 占用和内存峰值：

- [ ] 800×600 图片连续调色 30 秒
- [ ] 1920×1080 图片连续调色 30 秒
- [ ] 2560×1440 图片连续调色 30 秒
- [ ] 3024×1964 Retina 图片连续调色 30 秒
- [ ] 3840×2160 图片连续调色 30 秒
- [ ] 灰度单独调节性能
- [ ] 色相单独调节性能
- [ ] 灰度与色相组合调节性能
- [ ] 透明度单独调节性能
- [ ] 快速拖动滑块时 UI 不失去响应
- [ ] 连续打开、关闭控制面板 100 次不崩溃
- [ ] 连续创建并关闭浮窗 100 次，内存没有持续增长
- [ ] 运行 30 分钟后，程序仍能正常响应和退出

性能记录模板：

| 平台 | 图片尺寸 | 操作 | 平均耗时 | 最慢耗时 | 峰值内存 | 结果 |
|---|---:|---|---:|---:|---:|---|
| Win11 / 16 核 | 800x600 | 连续调色 30 秒（2459 帧） | 10.0 ms | 35.5 ms | +10 MB | 通过 |
| Win11 / 16 核 | 1920x1080 | 连续调色 30 秒（584 帧） | 42.8 ms | 86.8 ms | — | 通过 |
| Win11 / 16 核 | 2560x1440 | 连续调色 30 秒（339 帧） | 73.6 ms | 95.2 ms | — | 通过 |
| Win11 / 16 核 | 3024x1964 | 连续调色 30 秒（223 帧） | 117.9 ms | 138.5 ms | +74 MB | 通过 |
| Win11 / 16 核 | 3840x2160 | 连续调色 30 秒（157 帧） | 169.9 ms | 224.2 ms | +103 MB | 通过 |

内存列是单张图载入后的常驻增量，非泄漏判据；1920x1080 与 2560x1440 两行测得负值
（-7.7 / -36.1 MB），是分配器把内存还给了系统，故留空。
3840x2160 单张常驻约 103 MB，印证 README 已知限制中"全分辨率大图会保留多份像素缓存"。

---

## 10. 发布前最低验收标准

发布前必须满足：

- [ ] 自动化测试全部通过
- [ ] macOS 基础流程通过
- [ ] Windows 单显示器基础流程通过
- [ ] Windows 双显示器同 DPI 流程通过
- [ ] Windows 双显示器混合 DPI 流程通过
- [ ] 主屏和副屏截图均没有坐标偏移
- [ ] 灰度、色相、透明度及组合调节通过
- [ ] 所有 P0 / P1 缺陷已关闭
- [ ] 未关闭的 P2 / P3 缺陷已记录并确认可接受
- [ ] README 中的功能说明与实际行为一致

---

## 11. 测试结果记录

每次测试在下表新增一行：

| 日期 | Commit | 平台 | 显示器 / DPI | 自动测试 | 手工测试 | 结论 | 报告人 |
|---|---|---|---|---|---|---|---|
| 2026-08-11 | 9bb3de3 | Windows 11 23H2 | 双屏，混合 DPI 八组 | 65/65 通过 | 部分执行 | 通过，修复 4 项缺陷 | ye |

### 2026-08-11 Windows 第一轮

```text
测试日期：2026-08-11
测试人：ye
Git commit：9bb3de3
操作系统及版本：Windows 11 Home China 10.0.22631（23H2）
Python 版本：3.13.14（conda 环境 refpin，系统最高只有 3.10）
PySide6 版本：6.11.1（numpy 2.5.2 / Pillow 12.3.0 / mss 10.2.0）
CPU / 内存：16 逻辑核心
显示器数量：2
各显示器分辨率与缩放比例：
  27G7S-PRO   逻辑 2560x1440 @(0,0)，物理 3840x2160，150%（主屏）
  \\.\DISPLAY1 逻辑 1707x1067 @(-2560,0)，物理 2560x1600，150%（主屏左侧，负坐标）
显示器排列方式：副屏在主屏左侧
截图后端：qt 与 mss 均已对比测试
```

已通过（自动化）：

- 第 2 节 Windows 安装：依赖装齐，PyObjC 按平台标记正确跳过，无缺 DLL / Qt 插件 / NumPy wheel
- 第 3 节 自动化测试：imaging 33、floating 20、overlay 12，共 65 项全通过
  （本轮新增 4 项，覆盖 D-005 的双击复位与吸附、D-006 的物理像素换算与舍入）
- 第 3 节 静态检查：`compileall` 通过，`git diff --check` 通过
- 第 4 节 多显示器：负坐标副屏坐标换算正确（全局 -2460,80 → 局部 100,80）
- 第 4 节 截图正确性：qt 与 mss 两后端在两块屏上截同一区域，主屏内容差异 0/255、副屏 13.8/255
- 第 5 节 浮窗留屏：两块屏上窗口 1.6 秒后均停在原位，没有自动跳屏
- 第 6 节 面板开关与定位：双击开关、Esc 只关面板、跟随浮窗、四角不出屏、重开保值且不重算——均由 test_floating.py 覆盖
- 第 6 节 灰度 / 色相 / 透明度数值行为：由 test_imaging.py 与 test_floating.py 覆盖

已执行（手工）：

- 第 2 节 Windows 启动流程：可正常启动进入框选（启动耗时见 D-007）
- 第 6 节 控制面板全部交互项：仅带出 D-005 一条，其余未报异常
- 第 7 节 Windows 置顶行为：未报异常
- 第 7 节 混合 DPI 八组，缩放五档全覆盖（详见下方组合记录）
- 第 9 节 运行 30 分钟后仍能正常响应和退出：已执行，未报告异常
- 第 9 节 其余压力项：由 tools/stress_check.py 自动执行，三项全过
  - 五种尺寸各连续调色 30 秒，末段/首段耗时比在 0.94~1.03 之间，无劣化
  - 连续开关控制面板 100 次：前 20 次后增长 18.2 MB，此后 108.9 → 109.1 MB，
    四段采样基本持平，无泄漏
  - 连续创建并关闭浮窗 100 次：总增长 22.7 MB，但后半程仅 +2.0 MB，已趋平；
    前段增长来自缓存与线程池就位，非泄漏

未执行（本轮未做）：

- 第 4 节 部分框选与截图人工项（尺寸提示已在 D-006 中单独处理）
- 第 7 节 混合 DPI 中的主副屏互换
- 第 7 节 睡眠 / 锁屏 / 显示器热插拔恢复（见 D-008）
- 第 9 节 的 CPU 占用未记录（内存已由 tools/stress_check.py 采集）

缺陷：自动化部分见 D-001 ~ D-004，手工部分见 D-005 ~ D-008。

#### 混合 DPI 组合记录

改系统缩放**不需要注销**（Windows 10 1703+）。设置页那句"某些应用在你注销之前不会响应
缩放更改"针对的是非 per-monitor DPI 感知的程序；PinRef 未覆盖任何 DPI 设置，走 PySide6
6.11 默认的 Per-Monitor V2，重启进程即可拿到当前 DPI。已实测确认（见下表 scale 变化）。

| 主屏缩放 | 副屏缩放 | 主屏实测 scale | 副屏实测 scale | 主屏差异 | 副屏差异 | 结果 |
|---|---|---:|---:|---:|---:|---|
| 150% | 150% | 1.500 | 1.4996 | 0.0/255 | 13.8/255 | 通过 |
| 150% | 100% | 1.500 | 1.000 | 0.0/255 | 0.0/255 | 通过 |
| 100% | 150% | 1.000 | 1.500 | 0.0/255 | 19.0/255 | 通过 |
| 100% | 100% | 1.000 | 1.000 | 15.9/255 | 0.0/255 | 通过 |
| 150% | 200% | 1.500 | 2.000 | 0.0/255 | 0.0/255 | 通过 |
| 200% | 150% | 2.000 | 1.500 | 0.0/255 | 24.5/255 | 通过（触发 D-004，已修判据） |
| 175% | 125% | 1.750 | 1.250 | 0.0/255 | 11.3/255 | 通过（对齐 (0,0) / (+1,+1)） |
| 125% | 175% | 1.250 | 1.750 | 0.0/255 | 0.0/255 | 通过（两屏均对齐 (0,0)） |

表中"差异"为旧判据的平均绝对差，仅存档参考。改用对齐位移后复测：
主屏 (0,0)，副屏稳定 (+1,+1)，均通过。

两块屏的 scale 均为各自独立运行时推导，没有共用主屏的 devicePixelRatio，
即 `overlay.py:270` 那段的目标行为。副屏负坐标换算在三组下都正确
（全局 -2460,80 → 局部 100,80）。

第三组（副屏缩放高于主屏）是这三组里最有判别力的：若代码误用主屏 dpr 作全局值，
此时主屏 dpr=1.0，副屏抓屏会得到 320x240 而非正确的 480x360。实测副屏得到
480x360 / dpr 1.5，确认 scale 为每屏独立实测。

#### 如何解读"两个后端内容差异"

`tools/platform_check.py` 先后用 qt 和 mss 各抓一次再作比较。原先的判据是
逐像素平均绝对差配固定阈值 25，已因误报风险改掉（D-004），现在的判据是
**两张图的最佳对齐位移是不是 (0,0)**。平均绝对差仍会打印，但只作参考。

改判据的过程中查清了非零差异的真正来源，与最初的猜测不同，记录如下：

1. **两个后端算起点时用了两套乘数和两套舍入，差 1 物理像素。**
   注意：此处的解释前后修正过两次，最初记为"重采样模糊"，随后记为
   "非整数缩放导致的舍入"，两者都不成立——125% 是精确整数比却照样偏 1 像素。
   以下是经逐格验算、能覆盖全部七轮观测的版本：

   - `_grab_with_mss`（overlay.py）用 **_match_monitor 实测出的 scale** 配
     Python 内置 `round`，而 `round` 是银行家舍入（.5 取偶）。
   - Qt 的 `grabWindow` 用 **QScreen 声明的 devicePixelRatio** 配 `qRound`
     （.5 向上进位）。

   乘数不同、舍入规则也不同，于是乘积落在 .5 附近时两者给出差 1 的结果：

   | 配置 | 局部选区 | mss 起点 | qt 起点 | 偏移 |
   |---|---|---|---|---|
   | 副屏 125% | (682,426) | (852,532) | (853,533) | (+1,+1) |
   | 副屏 150% | (569,355) | (853,532) | (854,533) | (+1,+1) |
   | 主屏 175% | (731,411) | (1279,719) | (1279,719) | (0,0) |
   | 副屏 200% | (426,266) | (852,532) | (852,532) | (0,0) |
   | 副屏 100% | (853,533) | (853,533) | (853,533) | (0,0) |

   125% 组是纯舍入分歧：682 × 1.25 = 852.5 正好落在 .5，Python 取偶得 852，
   Qt 进位得 853。与是否整数比无关，1.25 是精确的。
   150% 组是乘数分歧：Qt 逻辑宽 1707 本身由 2560 / 1.5 = 1706.67 凑整而来，
   实测 scale 因而是 1.4997 而非 1.5，569 × 1.4997 = 853.33 对 569 × 1.5 = 853.5。

   证据一：在 (+1,+1) 处残差为 **0.0**——两张图除整体平移外逐像素相同，
   若是弥散的重采样模糊，残差不可能为零。

   证据二（预先声明的预测，事后核对成立）：把 125% 与 175% 在两块屏之间对调后，
   先按上述算法预测出两屏逻辑尺寸 3072x1728 与 1463x914、取样点 (1024,576) 与
   (487,304)、以及两屏均对齐 (0,0)，再实跑，全部命中。

   证据三：同为 125%，副屏（逻辑 2048x1280，取样 x=682，682×1.25=**852.5**）偏 1 像素，
   主屏（逻辑 3072x1728，取样 x=1024，1024×1.25=**1280.0**）却对齐。
   同一个缩放比给出不同结果，说明偏移取决于取样坐标的乘积是否落在 .5 上，
   而不是缩放比本身——"某个缩放比天生会偏"的说法就此排除。

   1 物理像素在 125%~150% 下不足 1 个逻辑像素，落到浮窗上不可见，判为可接受。
2. **两次抓屏之间画面变了。** 100/100 那轮主屏 scale 精确 1.000、位移 (0,0)，
   平均差却是 15.9，取样颜色数 21372 vs 22585 也对不上——主屏取样点 (1280,720)
   正是运行脚本的终端窗口位置，两次抓屏之间它在刷输出。

上述两项都会推高平均绝对差，且推高的幅度随画面细节量增长（同一副屏配置下
实测过 12.3 / 12.6 / 16.4 / 19.0 / 24.5），这正是固定阈值不可靠的原因。
对齐位移则不受这两项影响，同一配置下始终稳定给出 (+1,+1)。

测抓屏正确性时仍建议让取样区域保持静止且有内容：画面全平时对齐无从判定，
脚本会报"画面太平，无法判定对齐"并跳过该项，不计入通过。

同一个 320x240 逻辑选区在主副屏分别抓成 480x360（scale 1.5）和 640x480（scale 2.0），
逻辑尺寸均正确还原为 320x240，确认每屏按各自 scale 独立换算。

八组下来，缩放档位覆盖 100% / 125% / 150% / 175% / 200% 全部五档，
每一档在主屏和副屏上都各测过，高低关系两个方向也都测过，
scale 均为每屏独立实测，未发现串用。
第七、八组（175%+125% 与 125%+175%）补齐了单显示器 DPI 矩阵中缺的 125% 与 175%，
且互为对调，构成上述机制的对照实验。

**尚未覆盖的重要情况：副屏 y 坐标始终为 0。** 六组里两块屏都排在同一水平线上
（`@(0,0)` 与 `@(-2560,0)`），坐标换算中 y 方向的分支从未跑过非零值，
而 x 方向的负偏移已反复验证。建议在显示设置里把副屏拖到主屏上方（得到负 y）
和右侧（得到正 x）各测一次——只需拖动排列方块，不必改主屏，随手可还原。

其余待测：主副屏互换（价值最低、扰动最大，需要把 4K 主屏让给 2560x1600，
会打乱日常布局，故本轮未做）。

缺陷记录模板：

```text
标题：
严重程度：P0 / P1 / P2 / P3
平台及版本：
显示器与缩放：
截图后端：
前置条件：
复现步骤：
实际结果：
预期结果：
复现概率：
日志或截图：
关联 commit：
```

### 手工测试发现（2026-08-11 Windows 第一轮）

```text
标题：D-005 色相滑块极难精确回到 0
严重程度：P2
平台及版本：Windows 11 23H2
显示器与缩放：任意
截图后端：不适用
前置条件：浮窗上双击打开控制面板
复现步骤：拖动色相滑块，尝试停回 0°
实际结果：几乎无法停在 0，需反复微调。
         面板宽 268px（controls.py PANEL_WIDTH），扣掉边距与标签后滑槽约 150px，
         而色相取值有 361 个（-180~180），约 2.4°/像素，归零需要像素级精度。
         灰度默认 0、不透明度默认 100，默认值都在滑槽两端，随手可达；
         只有色相的默认值位于正中间，是三个滑块里唯一有此问题的。
预期结果：能可靠地回到默认值
复现概率：100%
日志或截图：无
关联 commit：9bb3de3
备注：候选方案——双击滑块复位到默认值（三个滑块一致处理），
     并在 0 附近加 ±3° 吸附。单靠吸附解决不了：±3° 仅约 1.2 像素，
     余量太小。方向键在滑块获得焦点后可步进 1°，但需先点中滑块，
     不能作为唯一手段。

状态：已修复（本轮）
修复方式：controls.py 新增 _ResettableSlider，三个滑块统一改用它。
  - 双击复位到该滑块的默认值（灰度 0 / 色相 0 / 不透明度 100）
  - 色相额外带 ±3° 吸附，灰度和不透明度不加：它们的默认值本就在滑槽两端
  吸附挂在 sliderMoved 而不是 valueChanged——后者对键盘也会发，
  挂上去的话从 0 按一下方向键到 1 会被吸回 0，方向键在默认值附近就废了。
  sliderMoved 只在鼠标拖动时发，正好只管住"拖不准"这一件事。
新增测试：test_double_click_resets_slider_to_default、
  test_hue_snaps_to_zero_only_while_dragging（后者专门断言方向键不被吸住）。
```

```text
标题：D-006 选框尺寸提示用逻辑像素，在高 DPI 屏上易被误读为截图掉了分辨率
严重程度：P3
平台及版本：Windows 11 23H2
显示器与缩放：4K 3840x2160 @150%
截图后端：qt
前置条件：无
复现步骤：在 4K 主屏上框选整屏，看选框旁的尺寸提示
实际结果：提示显示 "2560 × 1440"（overlay.py:102 用的是 box 的逻辑像素），
         使用者会以为没截到 4K。
         实测截图为 3840x2160 物理像素、dpr 1.5、浮窗逻辑尺寸 2560x1440，
         分辨率完整，没有任何裁切或降采样——属显示单位问题，不是截图缺陷。
预期结果：提示不应让人误判实际截图分辨率
复现概率：100%（任何非 100% 缩放的屏幕）
日志或截图：无
关联 commit：9bb3de3
备注：候选方案——非 100% 缩放时同时标出物理像素，如
     "2560 × 1440（3840 × 2160）"。需先确定这个工具该以哪种单位为准：
     逻辑像素对应"贴出来会占多大屏幕"，物理像素对应"参考图有多少细节"，
     两者对绘画用途都有意义。

状态：已修复（本轮）
修复方式：改为直接显示物理像素（不并列两个数，由使用者决定）。
  overlay.py 新增纯函数 _to_physical(logical, ratio)，_draw_size_hint 调用它。
  舍入用「加 0.5 再向下取整」而非内置 round：Python 的 round 是银行家舍入，
  round(2560.5) 得 2560，而 Qt 的 qRound 得 2561。副屏 150% 下逻辑宽 1707，
  ×1.5 正好落在 .5 上，用 round 会让标签比实际截到的少 1 像素
  （实测该屏整屏截图确为 2561x1601）。
新增测试：test_size_hint_reports_physical_pixels、test_size_hint_rounds_half_up_like_qt。
```

```text
标题：D-007 启动到可框选约 350~400 ms，有可感知延迟
严重程度：P3
平台及版本：Windows 11 23H2，16 逻辑核心，conda Python 3.13
显示器与缩放：任意
截图后端：不适用
前置条件：无
复现步骤：python -m pinref.main，观察从回车到遮罩出现的时间
实际结果：到可显示遮罩合计 318.7 ms，另加 Python 解释器自身启动，实测约 350~400 ms。
         分解：PySide6 import 214.2 ms（67%）、pinref import 69.7 ms（主要是 numpy）、
         QApplication 构造 34.6 ms、枚举屏幕 0.1 ms、ScreenSelector 构造 0.0 ms。
         即开销全在 import，与屏幕数量、DPI、代码逻辑无关。
预期结果：截图工具应接近即时响应
复现概率：100%
日志或截图：无
关联 commit：9bb3de3
备注：初判"只能省约 70 ms"，实测低估了。逐个量各依赖的导入成本后：
     numpy 153.6 ms、mss 61.6 ms、PySide6.QtWidgets 168.0 ms。
     numpy 与 mss 共 215 ms 在框选阶段都用不到，占启动路径一半以上。
     其中 mss 更是纯浪费——默认后端是 qt，绝大多数运行根本走不到那个分支，
     它却在 overlay.py 模块顶部无条件导入。

状态：已修复（本轮）
修复方式：
  1. overlay.py 的 import mss 挪进 _grab_with_mss 内部，只有真用 mss 后端才付这份钱。
  2. main.py 不再在模块顶部导入 pinref.floating（→ imaging → numpy），
     改为在 on_captured 里导入。
  3. 单纯延后只是把等待从启动挪到松开鼠标那一刻，两头都难受，
     所以 selector.start() 之后起一个 daemon 线程预热 pinref.floating。
     用户拖框的几百毫秒足够导完；线程内只做 import、不碰 Qt 对象，
     符合"GUI 对象只在主线程动"的约束；import 自带锁，与主线程撞上只会等待，
     不会重复执行。预热失败也不影响主流程——主线程用时会再导一次并正常报错。
实测（整个解释器进程的墙钟时间，各跑 5 次取中位）：
  改前 406.9 ms → 改后 217.8 ms，省 189 ms（快 46%）；解释器自身占 30.4 ms。
  预热线程耗时 219.4 ms（与用户拖框并行）；预热完成后主线程再导入仅 0.39 ms。
仍未解决：PySide6 的 ~168 ms 是硬成本，根本解法仍是阶段 3 常驻 + 全局快捷键。
```

```text
标题：D-008 睡眠 / 锁屏 / 显示器热插拔恢复未测试
严重程度：待定
平台及版本：Windows 11 23H2
显示器与缩放：双屏
截图后端：不适用
前置条件：无
复现步骤：未执行
实际结果：本轮未测试，TEST.md 第 7 节"从睡眠、锁屏或显示器热插拔恢复后，
         程序不崩溃"一项仍为空白。
预期结果：待测
复现概率：未知
日志或截图：无
关联 commit：9bb3de3
备注：此项与 DPI 变更相关联——睡眠恢复和热插拔都可能改变屏幕拓扑，
     而屏幕拓扑是在 ScreenSelector 构造时读取的，运行中拓扑变化是否被处理尚未验证。
```

```text
标题：D-009 Pillow 是声明了却从未被导入的依赖
严重程度：P3
平台及版本：不适用
显示器与缩放：不适用
截图后端：不适用
前置条件：无
复现步骤：grep -rn "PIL\|Pillow" pinref/ tests/
实际结果：代码中零引用。requirements.txt 将其注释为
         "图像读写与格式转换，配合 numpy 在 QPixmap 和数组之间来回转"，
         但 imaging.py 实际直接用 QImage 完成转换，未使用 Pillow。
         实测 import PIL.Image 需 91.2 ms，虽然当前没人导入它，
         但它仍是每次 pip install 都要装的一份依赖。
预期结果：依赖清单与实际使用一致
复现概率：100%
日志或截图：无
关联 commit：9bb3de3
备注：要么从 requirements.txt 移除，要么修正注释说明它是为哪个阶段预留的。
     本轮未改动——删依赖属于超出测试范围的决定，留给使用者定。
```

```text
标题：D-010 mss 后端算抓屏起点用 Python 的 round，与 Qt 的 qRound 不一致
严重程度：P3
平台及版本：Windows 11 23H2
显示器与缩放：任意非 100% 缩放，乘积落在 .5 附近时触发
截图后端：mss
前置条件：CAPTURE_BACKEND = "mss"
复现步骤：在 125% 或 150% 缩放的屏幕上抓一块起点使乘积落在 .5 的区域，
         与 qt 后端结果比对
实际结果：两后端起点相差 1 物理像素。overlay.py 的 _grab_with_mss 用内置 round，
         而 Python 的 round 是银行家舍入：round(852.5) 得 852。
         Qt 的 qRound 是四舍五入，得 853。
         此外 mss 用 _match_monitor 实测的 scale，Qt 用声明的 devicePixelRatio，
         两者在 150% 下分别是 1.4997 与 1.5，乘数本身也不同。
         逐格验算与七轮实测完全吻合（详见上方"如何解读"一节的表）。
预期结果：两后端应落在同一像素上
复现概率：特定坐标 100%
日志或截图：见混合 DPI 组合记录
关联 commit：9bb3de3
备注：影响 1 物理像素，在 125%~200% 下不足 1 个逻辑像素，浮窗上不可见，
     且两个后端是二选一使用、不会同时生效，故定为 P3。
     舍入这一半可修：把 round 换成与 _to_physical 相同的「加 0.5 再向下取整」，
     即可与 Qt 对齐，也与本轮 D-006 的修复保持一致。
     乘数那一半不宜动：实测 scale 是刻意设计（overlay.py 注释说明 macOS 上
     mss 按逻辑点抓屏、scale 为 1，不能写死成 devicePixelRatio），
     改用 dpr 会让 macOS 重新截偏。本轮未改动，等确认后再定。
```

### 已记录缺陷

```text
标题：D-001 大图调色在 Windows 上达不到实时，色相拖动约 6~7 fps
严重程度：P2
平台及版本：Windows 11 23H2，16 逻辑核心
显示器与缩放：27G7S-PRO 3840x2160 物理像素
截图后端：qt
前置条件：钉一张接近整屏的大图，打开控制面板
复现步骤：拖动色相滑块
实际结果：3840x2160（8.3M 像素）单帧 147.3 ms，色相+灰度 153.8 ms；
         2561x1601（4.1M 像素）单帧 73.5 ms。首次载入 755 ms。
         灰度拉满走捷径时 31.5 ms，这一路径正常。
         多线程相对单线程提速 4.9~5.0 倍，并行本身是生效的。
预期结果：commit 79a9cb1 记录 1600x1200 为 5.9 ms。按像素数线性外推，
         8.3M 像素应在 25 ms 上下，实测慢了约 6 倍。
         需要确认是 Windows 平台差异、CPU 差异，还是存在与图幅相关的非线性开销。
复现概率：100%
日志或截图：tools/platform_check.py 第 6 节输出
关联 commit：9bb3de3
```

```text
标题：D-002 ruff 0.16 默认规则集扩大后，tests/ 有 15 项检查不通过
严重程度：P3
平台及版本：ruff 0.16.2，与操作系统无关
显示器与缩放：不适用
截图后端：不适用
前置条件：无
复现步骤：ruff check pinref tests；ruff format --check pinref tests
实际结果：15 项，全部集中在 tests/，pinref/ 干净。
         RUF100 × 11：ruff 新默认集不含 E402，因此 `# noqa: E402` 全部成了无效指令
         I001 × 1：test_floating.py 导入块排序
         B008 × 1：test_floating.py:34 默认参数里调用 QPoint()
         PIE808 × 1、RUF007 × 1：test_imaging.py 的 range(0, 11) 和 zip 成对迭代
         format --check × 1：test_overlay.py:113 注释前多一个空格
         加 --isolated 结果相同，确认是 ruff 默认规则集变化，不是项目配置问题。
预期结果：静态检查全绿
复现概率：100%
日志或截图：见本轮测试输出
关联 commit：9bb3de3
备注：13 项可 --fix 自动修复。建议在仓库里加 ruff 配置固定规则集，
     否则 ruff 每次升级都可能让这项检查重新变红。
```

```text
标题：D-003 README 的 Python 版本与测试项数与实际不符
严重程度：P3
平台及版本：不适用
显示器与缩放：不适用
截图后端：不适用
前置条件：无
复现步骤：对照 README 与 requirements.txt、tests/ 实际内容
实际结果：README 写“Python 3.10 或更高版本”，但 requirements.txt 的 numpy>=2.3
         已不再发布 3.10 的 wheel，3.10 环境装不上依赖；
         README 写“共有 56 项自动化检查，选区与跨平台坐标 5 项”，
         实际为 61 项，overlay 为 10 项。
预期结果：文档与实际一致
复现概率：100%
日志或截图：无
关联 commit：9bb3de3
备注：本轮已修，同时补充了 conda 环境的安装说明和 tools/platform_check.py 的用法。
```

```text
标题：D-004 自检脚本的两后端差异阈值 25 会在非整数缩放下误报
严重程度：P2
平台及版本：Windows 11 23H2，tools/platform_check.py
显示器与缩放：副屏 2560x1600 @150%（实测 scale 1.4996），主屏 4K @200%
截图后端：qt 与 mss 对比
前置条件：副屏处于非整数缩放比，且取样区域画面细节丰富
复现步骤：python tools/platform_check.py，看第 4 节副屏一行
实际结果：差异 24.5/255，阈值为 25（platform_check.py:186 `same = diff < 25`），
         仅差 0.5 就会被判为"差异大！很可能有一个截偏了"并计入失败。
         此时取样颜色数 qt 与 mss 均为 524、完全相同，画面是静止的，
         差异全部来自 1.4996 的重采样，属于合法配置。
         同一副屏在画面较简单时（417 / 313 色）差异为 13.8 / 19.0，
         说明该差异随画面细节量增长，25 这个固定阈值没有安全余量。
预期结果：合法配置不应触发失败判定
复现概率：非整数缩放 + 高细节画面时稳定复现
日志或截图：本轮第 4 节输出
关联 commit：9bb3de3
备注：建议改为按 scale 是否为整数比分档设阈值，或改用结构相似度之类
     对重采样不敏感的比较方式，而不是固定的平均绝对差。
     注意不要简单调高阈值——那会同时降低真正"截偏"的检出能力。

状态：已修复（本轮）
修复方式：判据从"平均绝对差 < 25"改为"最佳对齐位移是否为 (0,0)"。
  在 ±6 像素内搜索让两张图最接近的整数位移，判据与"有没有截偏"这件事直接对应，
  因而不受重采样和画面变动影响，也就不必为噪点保留阈值余量。
  平均绝对差仍打印，标注为仅供参考。
  分档处理：
    - 对比度 < 3 → 画面太平，无法判定，不计失败
    - 残差 ≥ 对比度 × 0.5 → ±6 内根本对不上，报"超出搜索范围"而非给出假位移
    - 位移 (0,0) → 通过
    - 位移各分量绝对值 ≤ 1 → 非整数缩放的起点舍入，不足 1 个逻辑像素，通过
    - 其余 → 判为截偏，失败
反向验证：用合成图注入已知位移，确认判据没有退化成"永远通过"——
  0px 判通过，1px 判可接受，2px 与 4px 精确检出并判失败，
  9px 与 40px（超出搜索范围）报"对不上"而不给假数字，
  纯色图判为无法判定，两张无关图判失败。
```
